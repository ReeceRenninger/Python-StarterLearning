import shutil

#TODO: Copy File
shutil.copy('/home/<username>/file.txt', '/home/reece/copyFile.txt') # copies the file content to the new file path 
# this could also be used to copy a file to a new DIRECTORY 

shutil.copy('/home/<username>/file.txt', '/home/reece/CopiedStuff/newFileName.txt') # copies the file into a new directory AND renames the file


